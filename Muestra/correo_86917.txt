calculated in step 1). The bug in the currenct production portcalc is that in step 2), we didn't apply vol skew upon IF vol. In our new fix, this bug will be fixed. However, the IF volatility curves of those enclosed deals do not exist. So the current production version (3.15) used the NG vol + NG vol skew for those deals. The old version (3.14) used the NG vol for those deals. Thus you see P&L change in these two versions. Also we'd like to confirm with you that in the new fix, if the IF vol curve does