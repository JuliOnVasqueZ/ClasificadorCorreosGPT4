1 select ref_dt, strip_flag, sum(delta_position), sum(gamma) 2 from rms_open_position 3 where eff_dt = '&1' 4 and book_id = 'INTRA-WEST-PHY' 5 and nvl(strip_flag, 0) >= 0 6 group by ref_dt, strip_flag 7* having (sum(delta_position) > 0 or sum(gamma) > 0) Effective_dt 26-jan-01 REF_DT STRIP_FLAG SUM(DELTA_POSITION) SUM(GAMMA) Effective_dt 29-jan-01 REF_DT STRIP_FLAG SUM(DELTA_POSITION) SUM(GAMMA) REF_DT STRIP_FLAG SUM(DELTA_POSITION) SUM(GAMMA) REF_DT STRIP_FLAG SUM(DELTA_POSITION) SUM(GAMMA) The following is the list of sum of delta position on those dates without group by reference_dt: EFFECTIVE_DT STRIP_FLAG SUM(DELTA_POSITION) SUM(GAMMA) It looks to me the trend on VaR change is very similar to the trend on delta position change. If