my $working_dir; if ($^O =~ 'msys' || $^O =~ 'cygwin $working_dir = Win32::GetCwd $working_dir =~ tr } else { require Cwd; $working_dir = Cwd::cwd } return $working_dir; } #!/bin/sh # An example hook script to update a checked-out tree on a git push. # # This hook is invoked by git-receive-pack(1) when it reacts to git # push and updates reference(s) in its repository, and when the push # tries to update the branch that is currently checked out and the # receive.denyCurrentBranch configuration variable is set to # updateInstead. # # By default, such a push is refused if